services:
  - type: web
    name: videoflow
    runtime: docker
    plan: free
    healthCheckPath: /
    envVars:
      - key: VIDEOFLOW_HOSTED
        value: "1"
      - key: DATABASE_URL
        sync: false
